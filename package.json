{
  "name": "wildwood-online",
  "version": "2.0.0",
  "private": true,
  "description": "Wildwood multiplayer browser game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check engine.js && node --check client.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
